[Service]
# EnvironmentFile values override Environment= directives. Load the verified
# archive settings after the older emergency catch-up environment file.
EnvironmentFile=/etc/stellaratlas/archive-verifier-steady.env
